PG_USER=postgres
PG_PASSWORD=postgres
PG_NAME=app
PG_HOST=db
PG_PORT=5432

# Required by the API (no defaults in code). Generate real values, e.g.
#   python3 -c 'import secrets; print(secrets.token_hex(32))'
JWT_SECRET=change-me
